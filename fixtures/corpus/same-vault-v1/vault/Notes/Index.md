---
aliases:
  - "Start"
  - "Départ"
tags:
  - corpus
  - "#links"
status: active
---

# Index

The nearby target resolves before the duplicate basename elsewhere: [[Target|near alias]].
The root-qualified target resolves to [[/Root Target#Résumé|root alias]].
The Markdown form keeps its label and title: [a local target](Target.md#Section "hover").
The attachment is an ordinary vault file: ![[Attachments/diagram.svg|diagram]].
The note embed has a heading: ![[Target#Section]].
The block embed is relative to this note: ![[Target^stable-block]].
The unresolved target stays visible: [[Missing Note|not found]].
The duplicate basename is intentionally ambiguous: [[Duplicate]].

`[[Target]]` in inline code must not be indexed.
<!-- [ignored](Target.md), [[Target]], and ![[Attachments/diagram.svg]] are examples, not occurrences. -->

```md
[[Target]]
![[Attachments/diagram.svg]]
```
