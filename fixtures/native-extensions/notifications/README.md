# Native notification fixture

This source-only conformance fixture declares only `notifications` and sends one message through
the typed SDK. The suite supplies a host-owned adapter backed by a fake visible notice surface. It
does not access Electron, the DOM, the operating system, the network, or a durable notification
queue.
